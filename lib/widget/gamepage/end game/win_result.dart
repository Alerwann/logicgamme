import 'package:flutter/material.dart';

class WinResult extends StatelessWidget {
  const WinResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}